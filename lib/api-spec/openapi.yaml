openapi: 3.1.0
info:
  # Do not change the title, if the title changes, the import paths will be broken
  title: Api
  version: 0.1.0
  description: API specification
servers:
  - url: /api
    description: Base API path
tags:
  - name: health
    description: Health operations
  - name: whatsapp
    description: WhatsApp connection and number verification
paths:
  /healthz:
    get:
      operationId: healthCheck
      tags: [health]
      summary: Health check
      responses:
        "200":
          description: Healthy
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/HealthStatus"

  /wa/status:
    get:
      operationId: getWaStatus
      tags: [whatsapp]
      summary: Get WhatsApp connection status
      responses:
        "200":
          description: Status returned
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/WaStatus"

  /wa/disconnect:
    post:
      operationId: disconnectWa
      tags: [whatsapp]
      summary: Disconnect WhatsApp
      responses:
        "200":
          description: Disconnected
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/DisconnectResult"

  /wa/pairing-code:
    post:
      operationId: requestPairingCode
      tags: [whatsapp]
      summary: Request a pairing code for phone number login
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/PairingCodeInput"
      responses:
        "200":
          description: Pairing code generated
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PairingCodeResult"

  /wa/verify-number:
    post:
      operationId: verifyNumber
      tags: [whatsapp]
      summary: Verify a phone number on WhatsApp
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/NumberInput"
      responses:
        "200":
          description: Verification result
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/VerifyResult"

components:
  schemas:
    HealthStatus:
      type: object
      properties:
        status:
          type: string
      required:
        - status

    WaStatus:
      type: object
      properties:
        connected:
          type: boolean
        qr:
          type: ["string", "null"]
        phone:
          type: ["string", "null"]
        pairingCode:
          type: ["string", "null"]
      required:
        - connected

    DisconnectResult:
      type: object
      properties:
        success:
          type: boolean
        message:
          type: string
      required:
        - success
        - message

    PairingCodeInput:
      type: object
      properties:
        phone:
          type: string
          description: Phone number in international format e.g. 923001234567
      required:
        - phone

    PairingCodeResult:
      type: object
      properties:
        success:
          type: boolean
        code:
          type: ["string", "null"]
        message:
          type: string
      required:
        - success
        - message

    NumberInput:
      type: object
      properties:
        number:
          type: string
      required:
        - number

    VerifyResult:
      type: object
      properties:
        status:
          type: string
          enum: [active, deleted, error]
        reason:
          type: string
        number:
          type: string
      required:
        - status
        - reason
        - number
